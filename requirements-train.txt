# Everything in the served app, plus PyTorch for *fitting* the per-game LSTM
# (train.py, tools/convert_deep_artifacts.py, tools/verify_deep_parity.py).
# Not needed to run or deploy the app -- see requirements.txt.
#
# The CPU build is a ~200 MB wheel; the default PyPI wheel on Linux is ~2.5 GB of
# CUDA libraries this model has no use for. Install with:
#     pip install -r requirements-train.txt --extra-index-url https://download.pytorch.org/whl/cpu
-r requirements.txt
torch>=2.0,<3.0
